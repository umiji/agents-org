# 06. ドキュメントエージェント 役割定義

**Status:** Draft（論点をリサーチ後にまとめて確認）
**根拠:** 要件定義書 v0.1 §11 / §12 / §23
**前提:** 02 確定事項 B により、設計成果物は設計エージェントの持ち物。本ロールは**組織外向け**のドキュメントを担う。

---

## ミッション

開発成果物に必要なドキュメントを、最新状態に維持する。（§11）

## 責務

§11 の通り。README更新／仕様ドキュメント更新／API等の技術ドキュメント更新／開発者向けドキュメント更新／実装変更とドキュメントの整合性確認。

**担当範囲**（02 確定事項 B）:

| | ドキュメントエージェント | 設計エージェント |
| --- | --- | --- |
| 対象 | README、API文書、開発者向け文書、利用者向け仕様書 | 設計成果物 |
| 読み手 | 組織外（利用者・将来の開発者） | 組織内（実装・レビュー・テスト） |
| 書くこと | 現在どうなっているか、どう使うか | なぜその方式にしたか、却下した案 |

---

## 入力

### I-1. 作業指示（オーケストレーター → ドキュメントエージェント）

01 の O-2 の形式。

| 項目 | 内容 |
| --- | --- |
| 対象ドキュメント | 更新すべき文書。「実装変更に追随して判断せよ」も可 |
| 反映すべき変更 | どの実装タスクの結果を反映するか |
| 完了条件 | どこまで書けば完了か |

### I-2. 実装・設計の成果物

実装エージェントの完了報告（03 の O-2）、設計成果物（02 の O-1）、実際のコード。

**設計成果物は参照するが、書き写さない。** 読み手が違うため（上表）。

---

## 出力

### O-1. 更新されたドキュメント

### O-2. 整合性確認の結果

§11「実装変更とドキュメントの整合性確認」の成果。

| 項目 | 必須 | 内容 |
| --- | --- | --- |
| 確認した文書 | 必須 | |
| 実態とのずれ | 必須 | ずれている箇所。無ければ「無し」 |
| 更新したもの | 必須 | |
| 更新できなかったもの | 必須 | 判断が要る、範囲外等の理由付き |

### O-3. 完了報告（オーケストレーター向け）

### O-4. エスカレーション（→ オーケストレーター）

---

## エスカレーション条件（ドキュメントエージェント → オーケストレーター）

§11 にエスカレーション条件の記載が無いため、新たに定義する。

| # | 条件 | 判定の目安 |
| --- | --- | --- |
| E-1 | 実装の意図が読み取れない | コードからも設計からも、何のための機能か判断できない |
| E-2 | 実装とドキュメントのどちらが正しいか判断できない | 既存文書と実装が矛盾し、どちらが意図された姿か決められない |
| E-3 | 公開範囲の判断が要る | 外部公開する情報の線引き（認証情報、内部構成、未公開機能） |
| E-4 | ドキュメントの構成そのものを変える必要がある | 個別更新では追いつかず、体系の再編が要る |

E-2 が実務上もっとも起きる。ドキュメントエージェントは実装の下流にいるため、**過去の意図と現在の実装のずれ**を最初に発見する位置にある。これを黙って実装側に合わせて書き換えると、意図されない仕様変更が記録として正当化されてしまう。

---

## 論点（リサーチ後にまとめて確認）

### A. 起動タイミング

実装タスクごとにドキュメントタスクを起こすか、機能のまとまりごとにまとめるか、ゴール達成前にまとめて行うか。

こまめに行うと常に最新だがタスク数とトークンを消費する（§4.5）。まとめると効率的だが、間のセッションでドキュメントが実態から乖離する。

### B. ドキュメント更新の要否を誰が判断するか

実装タスクごとに「これはドキュメント更新が要るか」を判断する主体。オーケストレーターがタスク分解時に決めるか、ドキュメントエージェントが変更を見て判断するか、実装エージェントが完了報告で申告するか。

### C. MVPで維持するドキュメントの範囲

§11 は4種類（README / 仕様 / API等技術文書 / 開発者向け）を挙げるが、MVPで全てを維持する必要があるか。§3.1 は「開発に付随する仕様・設計・README等のドキュメント作成・更新」を対象としている。
