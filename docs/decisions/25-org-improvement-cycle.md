# 25. 組織自身を改善し続けるループ — 観測・診断・反映・追随

**Status:** Confirmed（**PO 承認 2026-09-01**。方向づけと §I の判断1 を PO が示した）
**この文書の位置づけ:** **`docs/decisions/22-org-self-improvement.md`（要求）に対する設計にあたる。** 22 は「組織が自分の運用課題を見つけ、PO へ改善案を出せなければならない」という PO の要求を、設計を決めないまま記録した文書である（Status に「設計は未確定」と明記され、末尾 D に6つの論点が残されていた）。**本記録はその6論点すべてに答える。** 対応表は末尾 §L にある
**PO の指示（2026-09-01）:** 「組織の課題を発見したら改善するサイクルを回せるようにしたい」「改善エージェントを立てつけて」「改善提案は専用台帳を新設するしかない」「各エージェントが都度課題を書き出し、改善エージェントが定期的に読んで、優先度の高いものを対応策とともにエスカレートし、承認されたら対応する流れ」「組織改善に関するナレッジは改善エージェントへ入れておくべき」
**根拠:** `docs/experiments/04-self-improving-org.md`（本リポジトリの現物調査 §1 と外部調査 §2）、要件定義書 v0.2 §6.2「層2の保守」/ §12.5 / §13.3 / §3.3 / §4.1 / §4.4 / §4.5 / §18、`docs/decisions/09`（停滞管理）/ `10`（ゴール健全性）/ `12`（エージェント追加方式）/ `14` 論点1（配布方式）/ `16`（トークン可視化）/ `20`（スキル割り当て）/ `21`（決定ログ索引）
**position:** 要件定義書 §23 が定めた12項目には無い。**PO が要求として述べた内容（22 に記録済み）に、設計を与える**ものである。
**実装:** **11件中7件が完了**（2026-09-02 時点。残るのは §H の 7・8・9・10）
**改訂:** 2026-09-02 に、2回目の外部調査（`docs/experiments/05-improvement-agent-practices.md`）を受けて改訂した。**変えたのは設計の骨格ではなく、改善エージェントの作り込みである**（§M）

---

## この文書が確定したこと

- **組織自身を改善するループを、4工程として定義した** — 観測 → 診断 → 反映 → 追随。既に散在していた部品を、この4工程のどこかへ割り当て直した
- **改善エージェント（`org-improvement`）を担当エージェントの6体目として足す。** 観測対象が「開発の成果物」ではなく「組織自身」である点で、既存5ロールのどれとも責務が重ならない
- **改善エージェントは自分では直さない。** 提案までを担当し、層2（組織自身への指示）への反映はオーケストレーターが行う。レビューエージェントと同じ形である
- **人間（PO）の承認でループを閉じる。** ただし全件を上げるとエスカレーション過多になるため、**反映の重さで3段に分けた**
- **担当エージェントが「組織の仕組みへの指摘」を、作業を止めずに残せる場所を作る** — タスク別ファイルの `## 組織への指摘` 節。エスカレーション（作業を止めて判断を仰ぐこと）とは別物として扱う
- **改善提案には「期待する効果」を必須欄にする。** どの指標がどう動けば成功かを書けない提案は受け付けない。**これが検証の最小形である**
- **定期実行の外部機構は作らない。** セッション開始時に走る検査へ「前回の組織点検から何件完了したか」を1行足し、点検の呼び出し時期が目に入るようにする
- **層2で閉じる改善と、配布物（層1）を直さないと全導入先で再発する改善を、提案の時点で区別する**

---

## A. なぜこれが要るのか

### A-1. 意図が言語化されないまま、部品だけが増えた

PO はこの組織の重要な性質として「課題を発見したら改善するサイクルを回せること」を挙げていた。**その要求は `docs/decisions/22-org-self-improvement.md` に記録されている**（2026-08-28。要求としては確定、設計は未確定）。**しかし要件定義書にも配布物にも、この意図は一度も反映されていない**——つまり要求は書き留められたが、組織の実体はそれを一度も持ったことがない（調査の詳細は `docs/experiments/04-self-improving-org.md` §1 事実1）。

**訂正の記録:** この節は当初「意図はどこにも記録されていない」と書いていた。**それは誤りである。** 本記録を書いたセッションは、22 が置かれたリモートの変更を取り込む前の状態で調査していたため、22 の存在に気づいていなかった。2026-09-01 の統合時に訂正した。

一方で、部品にあたるものは既にある——組織の異常を検知しうるものが7つ、見つけたものを反映する規約が3つ（同 事実2）。**つまり、目的が無いまま手段だけが個別の必要に応じて作られてきた状態である。** これは決定ログの索引を作ったとき（`docs/decisions/21-decision-log-index.md`）と同じ形の問題で、そのときの言葉を借りれば「書く費用は毎回かかるのに、効果が0」に近づく。

### A-2. 散在した部品では、拾えないものが4つある

| # | 拾えないもの | いま何が起きているか |
| --- | --- | --- |
| 1 | **2回目以降の点検** | 組織そのものの点検（`org/skills/org-first-run-check/SKILL.md`）は**最初のタスクが1件完了した時点で1回だけ**である。2回目をいつやるかが決まっていない。結果、「数タスク後に、ツールの実行許可の確認が何回出たかを見る」といった予定が、本リポジトリの `CLAUDE.md` に手書きのメモとして置かれているだけになっている |
| 2 | **層2 → 層1 の還流** | 開発対象リポジトリで見つかった不備を配布物へ戻す経路が、規約としては存在しない。**実害が出ている** — トライアル先が同じ不具合を独自に直し、**同じ問題を2箇所で別々に直した状態**になっている（`docs/handover.md` 現在地、`docs/decisions/14-open-questions.md` 論点1） |
| 3 | **組織の仕組みそのものへの異議** | エスカレーション（判断を上へ上げること）の4段階は、どれも**目の前のタスクをどう進めるか**を上げる経路である。「この規約には無理がある」「この道具が足りない」を、作業と独立に上げる先が無い |
| 4 | **改善の判断の記録先** | 決定記録（`docs/decisions/`）は層1だけの概念で、開発対象リポジトリには存在しない。改善をなぜそう決めたかを書く場所が無い（`docs/decisions/21-decision-log-index.md` が指摘済み） |

### A-3. 現状の引き金は「2回目」しか拾わない

層2の保守の引き金として明文化されているのは3つで、**いずれも「同じことが2回起きたら規約へ書け」の形である**（`org/skills/org-orchestrate/SKILL.md` 第8節）。1回目を拾う経路が書かれていない。1回目で拾えるものまで2回待つのは、単純に高くつく。

---

## B. 外の既成解から、何を採り、何を採らなかったか

調査の詳細は `docs/experiments/04-self-improving-org.md` §2。ここでは採否だけを書く。

### B-1. 採ったもの

| 出所 | 採ったもの | 本設計での現れ方 |
| --- | --- | --- |
| Warp（端末アプリを作っている会社。自社の公開リポジトリのコードレビュー等をエージェントに行わせている） | **観測役を、作業役とは別に立てる。** 作業のたびではなく、まとまった蓄積に対して動かす | 改善エージェントを6体目として足す（§D）。タスク単位では起動しない |
| 同 | **フィードバックは作業が起きている場所で受け取る。** 提出のための別作業を作らない | タスク別ファイルへ `## 組織への指摘` 節を足す（§E-2）。担当エージェントは完了報告のついでに1行書く |
| 同 | **提案は「最小の編集」にする**（the smallest edit that captured them） | 提案の書式に「どのファイルをどう直すか、最小の編集で」を含める（§F-2） |
| 同 | **人間の承認がループを閉じ、人が制御を保つ** | 反映を3段に分け、重いものは PO確認待ちキューへ載せる（§F-3） |
| 同 | **収集はスクリプトにやらせる。** 毎回コードを生成させない | 既存の3スクリプト（停滞検知・トークン集計・スキル収集）を観測の入力にする。新設は1本も要らず、既存1本の拡張で足りる（§H の7） |
| 同 | **フィードバックは量より質。** 詳しい人からの少数の具体的な指摘が、大量の雑な評価に勝る | **PO の差し戻し・却下・言い直しを最上位の信号として扱う**（§E-1 の信号8） |
| Anthropic「Building agents with the Claude Agent SDK」 | **ループは「文脈を集める → 行動する → 仕事を検証する → 繰り返す」。** 検証が信頼性の本体である | 4工程の設計そのもの（§C）。および「期待する効果」の必須化（§G） |
| 同 | **検証は規則ベースが最良。** どの規則がなぜ失敗したかを説明して返す | 改善が効いたかの判定に、**既存の数値指標**（ゴール健全性の8指標・手戻り回数・トークン消費）を使う（§G） |
| 同 | サブエージェント（親から呼ばれる子のセッション）は**独立した文脈領域**を持つ | 改善エージェントを担当エージェントとして立てる理由の1つ（§D-1） |
| Anthropic「Demystifying evals for AI agents」 | **経路ではなく結果を測る。** 道具の呼び出し順序のような手順の固定は硬すぎる | 改善の効果判定に、作業手順の遵守ではなく指標の変化を使う（§G） |
| 同 | **起こるべき場合と起こるべきでない場合の両方を見る。**「片側だけの評価は片側だけの最適化を生む」 | 提案の必須欄に「この変更で悪化しうるもの」を入れる（§F-2） |

### B-2. 採らなかったもの

| 採らなかったもの | 理由 |
| --- | --- |
| **提案の自動適用**（人間の承認を外す） | 出典の Warp も明示的に人間の承認でループを閉じている。加えて本組織では、層2は組織自身への指示であり、**壊れると全タスクへ波及する**。自動適用の失敗は、検知が最も遅れる種類の失敗である |
| **モデルによる判定を主判定に据える**（LLM-as-Judge。別の言語モデルに曖昧な基準で出来を判定させる方式） | 出典が「堅牢でなく、待ち時間の代償が重い」と明記している。改善が効いたかは**既存の数値指標で足りる**ため、わざわざ非決定的な判定を挟む理由が無い |
| **試験用課題の集合（eval）を20〜50件そろえる基盤の新設** | 出典は「実際に起きた失敗から取った20〜50件」を勧めているが、**トライアル先の完了タスクは数件規模で、母数が無い**。基盤だけ先に作っても埋まらない。§G の「期待する効果」の照合を先に回し、失敗事例が溜まってから判断する |
| **定期実行の外部機構**（スケジューラの導入） | 要件定義書 §3.3 が「Claude Code 新規セッションの完全自動起動」「24時間無人運転」を MVP 対象外としている。§C-5 の代替で足りる |
| **改善専用の設定ファイル（JSON / YAML）** | 要件定義書 §18 が禁じている。台帳は既存と同じ Markdown で書く |

---

## C. ループの定義 — 4工程

**「観測 → 診断 → 反映 → 追随」の4工程とする。** 前3つは Anthropic の「集める → 行動する → 検証する」に対応し、4つ目は**この組織に固有の工程**である——組織の定義が層1（本リポジトリ）と層2（各開発対象リポジトリ）に分かれているため、片方だけ直しても他の導入先では再発するためである。

| 工程 | 何をするか | 担当 | 起動条件 | 出力 |
| --- | --- | --- | --- | --- |
| **1. 観測** | 組織の異常を示す信号を集める。数え上げはスクリプト、読み取りは改善エージェント | 改善エージェント | §C-5 の3条件 | 信号の一覧 |
| **2. 診断** | 信号から原因を推定し、**最小の編集**として改善案を書く | 改善エージェント | 工程1の直後 | 改善提案（§F-2 の書式） |
| **3. 反映** | 提案を層2へ適用する。重さに応じて PO の承認を挟む | オーケストレーター（承認は PO） | 提案を受けた時点 | 層2の変更、または PO確認待ちキューの項目 |
| **4. 追随** | 層2で閉じない改善を、配布物（層1）へ戻す | オーケストレーター → PO → 本リポジトリ | 提案の適用範囲が「配布物」のとき | 本リポジトリへの差し戻し報告 |

### C-1. 観測・診断を、反映から切り離す

**これが要点である。** 反映する権限を持つ者が観測もすると、自分の失敗を見落とす。これは本リポジトリの既存の明文であり、2箇所で述べられている（`docs/experiments/04-self-improving-org.md` §1 事実6）。

> オーケストレーター自身が点検すると、自分の失敗を見落とすためである。（初回点検の手順）

> 書き換えられる側が自分を書き換えると、何が変わったのかを確かめる立場が消える。（導入時の初期シーケンス）

### C-2. 反映はオーケストレーターに残す

要件定義書 §6.2 が「層2の保守はオーケストレーターの責務」と定めている。**これを動かさない。** 改善エージェントに書き換えまで持たせると、§C-1 の分離が逆向きに壊れる（観測する者が反映もする）。

**レビューエージェントと同じ形である** — 指摘はするが、自分では直さない。

### C-3. 工程4（追随）が要る理由

層2は各開発対象リポジトリに置かれるコピーである。**そこだけを直すと、同じ組織を導入した他のリポジトリでは同じ問題が再発する。** これは既に起きている（§A-2 の2）。

**ただし配布方式そのものは、この文書では決めない。** 未決の論点として PO の判断待ちである（`docs/decisions/14-open-questions.md` 論点1）。ここで決めるのは、**「どの改善が配布物由来か」を提案の時点で機械的に拾える形にしておく**ことだけである。配布方式が決まったとき、その情報がそのまま使える。

### C-4. ループは「回す」のであって「回り続ける」のではない

**無人で回し続けることは狙わない。** 要件定義書 §3.3 が24時間無人運転を MVP 対象外としている。工程1が起動するのは §C-5 の3条件のときだけで、それ以外の時間、この仕組みは何もしない。

### C-5. 起動条件 — 定期実行の機構を作らずに定期性を得る

| # | 条件 | 意図 |
| --- | --- | --- |
| 1 | **最初のタスクが1件完了したとき**（現行の初回点検） | 導入直後の設定ミスを早く拾う。既存の手順をそのまま使う |
| 2 | **前回の組織点検から、タスクが N 件完了したとき** | 蓄積に対して動かす。N の既定値は §I の判断2 で PO に問う |
| 3 | **ゴール健全性評価の節目、または PO の指示** | 既に「節目ごと」と決まっている評価（`docs/decisions/10-goal-health.md`）に相乗りする |

**条件2を人が覚えていなくても分かるようにする。** セッション開始時に必ず走る検査（停滞検知・整合性検査 `org/scripts/org-check.py`）の出力へ、**「前回の組織点検から N 件完了している」の1行を足す**。これで、スケジューラを導入せずに呼び出し時期が目に入る。

**これは §4.1（Claude Code 標準機能を最大限使う）と §18（独自設定ファイルを作らない）に沿った選択である。** 外部のスケジューラも新しい設定ファイルも要らない。

---

## D. 改善エージェント（`org-improvement`）

### D-1. なぜロールを増やすのか

要件定義書 §17.3 と `docs/decisions/12-agent-addition.md` は「**ロールを増やす前に確認する。専門知識が要るだけならスキルを足す。ロールは責務の単位であって、知識の単位ではない**」と定めている。この確認に答える。

| 問い | 答え |
| --- | --- |
| 既存5ロールのどれかに入らないか | **入らない。** 5ロールはいずれも**開発の成果物**（設計・実装・レビュー・テスト・利用者向け文書）を対象にしている。改善エージェントの対象は**組織自身**であり、成果物ではない |
| オーケストレーターに持たせられないか | **持たせられない。** ①自分で自分を点検すると失敗を見落とす（§C-1 の既存明文）。②オーケストレーターの文脈は報告を受けるたびに膨張することが調査で分かっている（`docs/decisions/00-research-notes.md`）。観測は台帳・作業ログ・集計結果を横断して読むため、最も文脈を食う作業である |
| 知識が足りないだけではないのか | **違う。** 足りないのは知識ではなく**立場**である。既存の誰も「組織自身を対象にする」責務を負っていない |
| 既存ロールとの境界を一文で言えるか | **言える。**「レビューエージェントは成果物を見る。改善エージェントは組織を見る」 |

**加えて、サブエージェントは独立した文脈領域を持つ**という性質が、この用途に合う（`docs/experiments/04-self-improving-org.md` §2.2）。大量に読み、結論だけを親へ返す作業に向いている。

### D-2. 定義の5要素

`docs/decisions/12-agent-addition.md` が定めた必須構成（ミッション／責務／入力／出力／エスカレーション条件）に従う。

**ミッション**

組織が最大の成果を出すことを妨げているものを見つけ、**それをどう直すかの提案**を作る。開発の成果物には触れない。

**責務 — 担当すること**

- 観測（工程1）— 信号を集め、異常を特定する
- 診断（工程2）— 原因を推定し、**最小の編集**として改善案を書く
- 効果の照合 — 前回の提案が適用されていた場合、**期待した効果が出たかを指標で照合する**（§G）
- アセットの不足の検知 — この実行環境から呼べるスキルと、このリポジトリの技術構成を突き合わせ、足りないものを挙げる（導入時の初期シーケンス `org/skills/org-init/SKILL.md` の段1・段2 の再実行にあたる）

**責務 — 担当しないこと**（要件定義書 §6.3 が「やらないこと」を明示したのと同じ形）

- **層2（`CLAUDE.md`・`.claude/rules/`・エージェント定義・用語集）を自分で書き換えない。** 提案までである
- **開発の成果物に触れない。** コード・テスト・設計成果物・利用者向け文書のいずれも対象外
- **個々の担当エージェントの出来を評定しない。** 対象は仕組みであって、担当者ではない
- **タスクを登録・割り当てしない。** 登録はオーケストレーターの責務（要件定義書 §6.2）

**入力**

| # | 何を | どこから |
| --- | --- | --- |
| I-1 | 起動理由（§C-5 のどれか）と、前回の組織点検の時点 | オーケストレーター |
| I-2 | タスク台帳（索引 `docs/task-list-{project-name}.csv` と、タスク別ファイル `docs/tasks/T-XXX.md`） | 開発対象リポジトリ |
| I-3 | 停滞検知・整合性検査とゴール健全性の集計結果 | `org/scripts/org-check.py` の出力 |
| I-4 | トークン消費の集計結果 | `org/scripts/org-tokens.py` の出力 |
| I-5 | この実行環境から呼べるスキルの一覧 | `org/scripts/org-skills.py` の出力 |
| I-6 | PO確認待ちキュー（`docs/po-queue.md`） | 開発対象リポジトリ |
| I-7 | 前回までの改善提案とその状態 | 改善提案の台帳（§F-1） |

**出力**

| # | 何を | 誰へ |
| --- | --- | --- |
| O-1 | 改善提案（0件のこともある。§F-2 の書式） | オーケストレーター |
| O-2 | 前回提案の効果の照合結果（効いた／効かなかった／まだ判定できない） | オーケストレーター |
| O-3 | 観測したが提案に至らなかった信号と、その理由 | オーケストレーター |

**O-3 を必須にする理由。** 何も出さないと「見ていないのか、見たうえで問題無しなのか」が区別できない。**判定できなかったこと自体が信号である。**

**エスカレーション条件 — 上げるもの**

- 観測に必要なファイルが存在しない、または壊れていて読めない
- 提案が**ゴール自体の見直し**を要する（ゴール健全性評価の「ゴールが揺らいでいる」に相当。`docs/decisions/10-goal-health.md`）
- 同じ問題を**2回続けて提案し、2回とも反映されなかった**

**エスカレーション条件 — 上げないもの**

- 提案が採用されなかったこと（採否はオーケストレーターと PO の判断であり、差し戻しではない）
- 信号が0件だったこと（O-3 で報告すれば足りる）

### D-3. 直接通信は許さない

`docs/decisions/12-agent-addition.md` は「**直接通信の経路を増やすときだけは摩擦を残す。ファイル追加では済ませず、組織規約の変更として扱う**」と定めている。

**改善エージェントに直接通信の例外は与えない。** 返す先はオーケストレーターだけである。承認済みの3経路（レビュー→実装、実装→設計、テスト→実装）はいずれも「実装への差し戻しであり、設計や要件を変えない」という共通の性質を持つが、**改善の提案は組織の規約を変えるものであり、この性質を満たさない**。

---

## E. 観測する信号

### E-1. 何を集めるか

**大半は既存の仕組みから機械的に取れる。** 新しく作るのは信号7と8だけである。

| # | 信号 | 出所 | 何を疑うか |
| --- | --- | --- | --- |
| 1 | 実時間基準の停滞（更新が2日以上止まっている） | `org-check.py` | 指示の不足、依存の見落とし |
| 2 | サイクル基準の停滞（手戻り回数3以上） | 同（タスク別ファイルの `手戻り回数`） | 完了条件が曖昧、設計の欠陥 |
| 3 | ボトルネックとなっている工程（状態別の滞留日数の中央値） | `org-check.py --summary` の指標7 | その工程の担当への指示不足、道具の不足 |
| 4 | エスカレーションの偏り（同じ論点が複数タスクから／特定のロールからばかり） | `docs/po-queue.md` の横断 | 層2に判断基準が無い。回答の還元を怠っている（要件定義書 §12.5 の「症状 → 疑うべき原因」の対応表がそのまま使える） |
| 5 | PO確認待ちの滞留（未回答が2日以上） | `org-check.py` | ゴール達成条件が確定していない |
| 6 | トークン消費の偏り（特定の担当・モデル・タスクへの集中） | `org-tokens.py --by agent / --by model / --by task` | 手順の冗長、委譲の設計ミス |
| 7 | **担当エージェントからの、組織の仕組みへの指摘** | **新設**（§E-2） | 規約の無理、道具・アセットの不足 |
| 8 | **PO の差し戻し・却下・言い直し** | **新設**（§E-3） | 最も価値の高い信号。§B-1 のとおり、量より質である |
| 9 | 呼べるスキルと技術構成の食い違い | `org-skills.py` ＋ 依存関係の宣言ファイル | アセットの不足（`docs/decisions/20-skill-assignment.md`） |

### E-2. 信号7 — 担当エージェントが、作業を止めずに指摘を残す場所

**タスク別ファイル `docs/tasks/T-XXX.md` へ `## 組織への指摘` 節を足す。** 担当エージェントは、完了報告を書くついでにここへ1行書く。

**これはエスカレーションではない。** エスカレーションは「判断できないので作業を止めて仰ぐ」ことであり、4段階の経路がある（`docs/decisions/08-escalation-criteria.md`）。ここに書くのは**作業を止めずに残す一方向の記録**であり、返事を期待しない。

書くものは3種に限る（何でも書ける欄にすると読めなくなる）。

| 種別 | 書く条件 |
| --- | --- |
| `規約` | 層2の規約に従うと作業が進まない、または矛盾する箇所に当たった |
| `道具` | 必要な道具・スキル・外部アクセスが無く、遠回りをした |
| `反復` | 同じ種類の手戻りを、このタスクで2回以上経験した |

**書式は1行1件で `- [種別] 内容` とする。** 機械が拾うためである。**書式を文章でだけ規定して雛形を配らないのは、本リポジトリが既に踏んだ失敗である**（`docs/handover.md` 末尾の教訓）。タスク別ファイルの雛形 `org/templates/T-XXX.md` へ、この節を実物として入れる。

**Warp の「フィードバックは作業が起きている場所で受け取る。提出のための別作業を作らない」を、この形で実現している**（§B-1）。専用の報告先を作ると、報告そのものが仕事になり、書かれなくなる。

### E-3. 信号8 — PO の反応を残す

**PO の差し戻し・却下・言い直しが、最も価値の高い信号である**（§B-1）。しかし現状、PO の反応は会話の中にしか無く、セッションが終われば消える。

**PO確認待ちキュー（`docs/po-queue.md`）の各項目へ `反応` 欄を1行足す。** 回答そのものとは別に、**「なぜその回答になったか」「問いのどこが悪かったか」**を、オーケストレーターが PO の言葉に近い形で残す。

要件定義書 §13.3「回答の還元」が既に「一度の回答を、以後の判断基準にする」と定めている。**この欄はその入力を残すためのものであり、新しい規約ではなく既存規約の実行部分である。**

---

## F. 提案と反映

### F-1. 台帳 `docs/org-improvements.md` を新設する（**PO 承認 2026-09-01**）

改善は、セッションをまたいで残らなければならない（起票 → 診断 → 承認 → 適用 → 効果の照合、が複数セッションにまたがるため）。**専用の台帳を1つ新設する。** 開発対象リポジトリに置くファイルの7種類目になる。

**1課題1ブロックとし、課題の一生をそのブロックの中で進める。** 課題の台帳と提案の台帳を分けない——分けると、どの提案がどの課題に対するものかを人が突き合わせることになる。

| 状態 | 意味 | 次に動かすのは誰か |
| --- | --- | --- |
| `未処理` | 起票されたが、まだ誰も診ていない | 改善エージェント（次の点検で） |
| `診断済み` | 原因の推定と対応策が書かれた。優先度がついた | オーケストレーター |
| `承認待ち` | PO確認待ちキューへ載せた（重さが `中` か `重` のもの） | PO |
| `承認済み` | PO が承認した。まだ適用していない | オーケストレーター |
| `適用済み` | 層2へ反映した。効果はまだ見ていない | 改善エージェント（次の点検で照合） |
| `効果確認済み` | §G の照合で「効いた」と判定した。**ここで閉じる** | — |
| `却下` | 採らないと決めた。**理由を必ず書く** | — |
| `見送り` | 今は対処しないが、消さない。再燃したら `未処理` へ戻す | — |

### F-1-a. 台帳へ書いてよいのは誰か — **担当エージェントは直接書かない**

**PO が示した流れ**（各エージェントが台帳へ都度書き出し、改善エージェントが定期的に読む）**のうち、「台帳へ直接書く」部分だけを変える。** 理由は、既に確定している並列実行の規約と正面から衝突するためである。

> **並列に走らせてよいのは、触るファイルが重ならないタスクだけ。**（`docs/decisions/15-branch-and-parallel.md`）

担当エージェント全員が1つの台帳へ書くと、**その台帳は全タスクが触るファイルになり、並列実行が構造的に成立しなくなる。** 加えて、同時に走る2体が同じファイルへ書けば、片方の追記が消える。

**既存の共有ファイルは、すべて同じ方法でこれを避けている** — タスク索引の CSV も、PO確認待ちキューも、**書くのはオーケストレーターだけ**という不変条件で守られている（`docs/decisions/07-task-management.md`）。台帳もこれに合わせる。

| 誰が | 台帳へ書くか | 何を書くか |
| --- | --- | --- |
| **担当エージェント**（設計・実装・レビュー・テスト・ドキュメント） | **書かない** | 自分のタスクファイルの `## 組織への指摘` へ1行書き、**完了報告にも含める**（§E-2）。**自分しか触らないファイルなので衝突しない** |
| **オーケストレーター** | **書く（唯一の書き手）** | 担当エージェントから吸い上げた指摘を**1課題1レコードにまとめて起票**する。以後の状態の更新、診断結果の転記、PO の回答、適用の記録も行う |
| **改善エージェント** | **書かない** | 診断結果はオーケストレーターへ返す。編集系ツールを持たない（§D-2） |
| **スクリプト**（`org-check.py`） | **書かない** | **タスクファイルに書かれているのに台帳へ起票されていない指摘**を検出して警告する。起票そのものはしない |

**「まとめる」は判断であり、機械にはできない。** 3つの別々のタスクから同じ趣旨の指摘が出たとき、それを**1レコードに束ねるか、別々の課題として立てるか**の判断が要る。機械に集約させると3レコードになり、優先度づけの基準（「同じ課題が3件以上の別タスクから出ている」）が働かなくなる。**両者の報告を受け取るオーケストレーターだけが、この判断ができる位置にいる。**

**スクリプトに書かせないもう1つの理由。** 停滞検知・整合性検査のスクリプト（`org-check.py`）は、**読んで警告を出すだけで何も書かない道具**である。ここへ書き込みを足すと、その性格が壊れる。**検出だけを足す**——タスクファイルに書かれているのに台帳に無い指摘があれば警告する。これは既存の整合性検査と同じ性質の機能である。

**PO の流れの意図は変わっていない。** 「各エージェントが課題と感じたことを都度書き出す」はそのままで、書き出す先が自分のタスクファイルと完了報告になり、**台帳への集約をオーケストレーターが行う**。

**タスクファイルの節と台帳の関係。** タスクファイルの `## 組織への指摘` が**担当エージェントが書いた原文（正典）**、台帳が**まとめた作業台**である。これは既存の「タスク別ファイルが正典で、CSV が索引」という関係と同じ形である。

**取りこぼしへの備え。** オーケストレーターが起票を怠っても、原文はタスクファイルに残る。**改善エージェントは点検のたびに、台帳に無い指摘が残っていないかを確かめる**（スクリプトの警告と、自分での横断検索の両方で）。

**タスクの外で気づいた課題**（オーケストレーター自身が気づいたもの、PO の反応から拾ったもの）も、オーケストレーターが直接起票する。

### F-1-b. 優先度は改善エージェントがつける

PO の流れどおり、**改善エージェントは全件を上げず、優先度の高いものを対応策とともにエスカレートする。** 3値とする。

| 優先度 | 基準 |
| --- | --- |
| **高** | 組織が止まっている、または止まりかけている（停滞・ブロックの原因になっている）／同じ課題が**3件以上の別タスク**から出ている／**PO の反応（信号8）に由来する** |
| **中** | 手戻りやトークンの無駄が、測れる形で出ている |
| **低** | 不便ではあるが、作業は進んでいる |

**優先度を件数だけで決めない。** Warp の「フィードバックは量より質」（§B-1）に従い、**PO 由来の指摘1件は、担当エージェント由来の3件より重く扱う。**

**1回の点検でエスカレートするのは最大3件とする。** 全件を上げると、この仕組み自体が「エスカレーションが多すぎる」という症状の原因になる（要件定義書 §12.5）。残りは台帳に `未処理` のまま残り、次の点検で再び優先度づけの対象になる。

### F-2. 提案の書式

**1提案1ブロック。** 欄は7つで、**すべて必須**とする。

| 欄 | 内容 | なぜ必須か |
| --- | --- | --- |
| `信号` | どの信号（§E-1 の1〜9）から、何件観測したか | 推測で提案させないため |
| `原因の推定` | なぜそれが起きていると考えるか | 症状への対症療法を防ぐ |
| `変更` | **どのファイルを、どう直すか。最小の編集で書く** | Warp の「最小の編集」（§B-1）。大きい変更は検証できない |
| `適用範囲` | `このリポジトリのみ` / `配布物` のいずれか | 工程4（追随）が機械的に拾えるようにするため（§C-3） |
| `重さ` | `軽` / `中` / `重` のいずれか（§F-3） | 承認経路が決まる |
| `期待する効果` | **どの指標がどう動けば成功か** | §G。書けない提案は検証できない |
| `悪化しうるもの` | この変更で悪くなる可能性があるもの | 「片側だけの評価は片側だけの最適化を生む」（§B-1） |

### F-3. 反映の3段 — 承認をどこまで PO へ上げるか

**全件を PO へ上げると、この仕組み自体がエスカレーション過多の原因になる**（要件定義書 §12.5 が挙げる症状そのものである）。重さで分ける。

| 重さ | 該当するもの | 承認 | 記録 |
| --- | --- | --- | --- |
| **軽** | 層2の規約への追記、用語集への語の登録、判断してよい範囲の明文化、ビルド・テストのコマンドの修正 | **不要。** オーケストレーターが反映し、**事後に PO へ1行で報告する** | 改善提案の台帳に「適用済み」として残す |
| **中** | エージェント定義の変更、スキル割り当ての変更、停滞判定などの閾値の変更、タスク別ファイルの書式変更 | **PO確認待ちキューへ載せる** | 同上 ＋ PO確認待ちキューの `Q-XXX` |
| **重** | ロールの追加・削除、直接通信の経路の追加、配布物（層1）の変更、ゴール自体の見直し | **PO確認待ちキューへ載せる。** さらに層1では**決定記録が要る** | 同上 ＋ 本リポジトリの `docs/decisions/` |

**軽を事後報告で通す根拠。** 要件定義書 §6.2 が層2の保守をオーケストレーターの責務として既に与えており、そこに PO の事前承認は要求されていない。**この文書はその範囲を広げない。**

**重に「配布物の変更」が入る理由。** 層1の変更は全導入先へ波及する。`docs/decisions/12-agent-addition.md` が直接通信経路の追加について「ファイル追加では済ませず、組織規約の変更として扱う」と定めたのと同じ趣旨である。

---

## G. 検証 — 改善が効いたかをどう見るか

**「期待する効果」欄が、この組織における eval（試験用課題の集合。エージェントの出来を測るための課題）の代わりである。**

提案を出す時点で「どの指標がどう動けば成功か」を書かせ、**次の組織点検で照合する。** 照合に使うのは、すべて既に集計できる数値である。

| 使う指標 | 出所 |
| --- | --- |
| ゴール健全性の8指標（完了・未完了・停滞・ブロック中・PO確認待ち・手戻り件数・ボトルネック工程・残作業） | `org-check.py --summary`（`docs/decisions/10-goal-health.md`） |
| 手戻り回数（タスク別） | タスク別ファイルのメタデータ |
| トークン消費（担当別・モデル別・タスク別） | `org-tokens.py` |
| 組織への指摘の件数（種別ごと） | §E-2 の新設節 |

照合の結果は3値とする。

| 判定 | 意味 | 次にすること |
| --- | --- | --- |
| **効いた** | 期待した指標が期待した向きに動いた | 提案を「確定」にする |
| **効かなかった** | 動かなかった、または逆に動いた | **変更を戻すか、原因の推定をやり直す。上乗せで別の変更を積まない** |
| **まだ判定できない** | 母数が足りない（対象の工程をまだ通っていない等） | 次回へ持ち越す。**持ち越し2回で、判定できない理由を報告する** |

**「効かなかった」で上乗せを禁じる理由。** 効かない変更の上に別の変更を積むと、層2が誰も意図していない規約の堆積物になる。これは停滞管理が「同じ手を繰り返すのは停滞の解消ではなく停滞の継続である」と述べたのと同じ構造である（`docs/decisions/09-stagnation.md`）。

**モデルによる判定（LLM-as-Judge）は使わない。** 理由は §B-2 のとおり。

---

## H. 作るもの

作るものは11件。**うち7件（1・2・3・4・5・6・11）が実装済みである**（5件を 2026-09-01 に、2件を 2026-09-02 に）。

| 実装済み | ファイル | いつ |
| --- | --- | --- |
| 改善エージェントの定義 | `org/agents/org-improvement.md` | 09-01（09-02 に §M-1・M-2 で作り込み） |
| 観測と診断の手順（**組織改善のナレッジはここに入れた**） | `org/skills/org-improve/SKILL.md` | 09-01（09-02 に §M-1 で作り込み） |
| 組織改善の台帳の雛形 | `org/templates/org-improvements.md` | 09-01（09-02 に `撤回` 追加） |
| タスク別ファイルへ `## 組織への指摘` 節 | `org/templates/T-XXX.md` | 09-01（09-02 に `ロール` 追加） |
| PO確認待ちキューへ `反応` 欄 | `org/templates/po-queue.md` | 09-01 |
| **組織への指摘の書き方（層2の規約）** | `org/rules/org-improvement-feedback.md` | **09-02** |
| **配布物の一覧と、層2の運営規約への周知** | `org/README.md` / `org/CLAUDE.md` | **09-02** |

**残る4件は 7・8・9・10 である。** このうち **10（オーケストレーターの手順の改訂）が本体**である——**担当エージェントから指摘を吸い上げ、1課題1レコードにまとめて台帳へ起票する**手順が、まだどこにも書かれていない。**これが無いと、担当エージェントは書けるが誰も台帳へ載せない状態のままである。** 7（スクリプトの拡張）はその取りこぼしを検出する備えであり、10 の後に効く。

**7 を実装するとき、`ロール` 種別が増えたことに注意する**（§M-1 の3）。指摘を拾う際の種別は4つになっている。

| # | 作るもの | 内容 | 依存 |
| --- | --- | --- | --- |
| 1 | `org/agents/org-improvement.md` | 改善エージェントの定義。§D の5要素。使えるツールは読み取り系（`Read` `Grep` `Glob` `Bash`）と `Skill` のみ。**編集系は与えない**（§D-2 の「自分では書き換えない」を構造で担保する） | §I 判断1・2 |
| 2 | `org/skills/org-improve/SKILL.md` | 観測と診断の手順書。**信号10種**の集め方、7段のはしご（段3.5）、提案の書式、組織の目標状態（段5.5）、効果の照合の手順 | 同上 |
| 3 | 改善提案の台帳（置き場所は §I 判断1）と、その雛形 | 提案の実体 | §I 判断1 |
| 4 | `org/templates/T-XXX.md` へ `## 組織への指摘` 節を追加 | §E-2。**実物の形で入れる。**文章での規定だけにしない | — |
| 5 | ~~`org/rules/` へ、組織への指摘の書き方を追加~~ **→ 完了（2026-09-02）** | `org/rules/org-improvement-feedback.md`。**4種別**と1行1件の書式、`ロール` の6つの引き金、7段のはしご、なぜ書くのか | 4 |
| 6 | `org/templates/` の PO確認待ちキューへ `反応` 欄を追加 | §E-3 | — |
| 7 | `org/scripts/org-check.py` の拡張 | ①「前回の組織点検から N 件完了」の1行を出す（§C-5）。②**タスクファイルに書かれているのに台帳へ起票されていない指摘**を警告する（§F-1-a）。**書き込みはしない** | 4 |
| 8 | `tests/test_org_check.py` への回帰テスト追加 | 7 の2機能。**配布物のスクリプトには回帰テストを揃える**という本リポジトリの既定に従う | 7 |
| 9 | `org/skills/org-first-run-check/SKILL.md` の改訂 | 「1回だけ」から「§C-5 の3条件で繰り返す」へ。2回目以降は改善エージェントが行う | 1, 2 |
| 10 | `org/skills/org-orchestrate/SKILL.md` の改訂 | 第8節「層2を保守する」へ、改善エージェントからの提案を受けて反映する経路と、3段の承認（§F-3）を追加 | 1, 2, 3 |
| 11 | ~~`org/README.md` と `org/CLAUDE.md` の改訂~~ **→ 完了（2026-09-02）** | 配布物の一覧へ4件（改善エージェント・手順スキル・台帳の雛形・指摘の規約）。**このうち3件は、それまで一覧に載っておらず、手動コピーの手順では届かない状態だった。** 層2の運営規約へは入口の3行のみ | — |

**配布物にはバージョンの刻印が無い。** 工程4（追随）を成立させるには、導入先が「自分がどの版か」を言えなければならない。**これは配布方式そのものの論点であり、`docs/decisions/24-distribution-plugin.md`（配布方式をプラグイン機構へ移す。PO 判断 2026-08-28）で決着している。** 刻印は移行の中で扱う（§I の判断3）。

---

## I. PO の判断が要る点

**推測で埋めない**（本リポジトリの確定作業の規約）。当初3件あったが、**残っているのは判断2（点検の起動間隔）の追認だけである。**

### 判断1. 改善提案をどこに置くか → **決着（PO 承認 2026-09-01）**

**専用の台帳 `docs/org-improvements.md` を新設する。** 開発対象リポジトリに置くファイルの7種類目になる。既存のタスク台帳への相乗りは採らない——ゴール健全性評価（`docs/decisions/10-goal-health.md`）は「タスクは消化されているのにゴールへ近づいていない」を検知するために作られており、そこへ組織改善のタスクが混ざると、まさにその検知が効かなくなるためである。

**台帳の設計は §F-1 にある。** PO が示した流れのうち「担当エージェントが台帳へ直接書く」部分だけは、並列実行の規約と衝突するため、**タスクファイルへ書いてスクリプトが集約する形**へ置き換えた（§F-1-a）。意図は変えていない。

### 判断2. 組織点検の起動間隔（§C-5 の条件2の N）→ **暫定で 5 を採用。PO の明示的な回答は未取得**

| 案 | N | 利害 |
| --- | --- | --- |
| **A（推奨）** | **5件** | 信号が溜まる程度に大きく、問題が固着する前に拾える程度に小さい。トライアル先の現在の規模（完了数件）では**まだ1度も起動しない**——それでよい。母数の無い観測は当てにならない |
| B | 3件 | 早く回るが、母数が足りず、提案が推測になりやすい |
| C | 10件 | 提案の質は上がるが、1回目の起動までが遠い |

**A を推す。** ただし**これは暫定値であり、実測で見直す。** 停滞判定の閾値（手戻り回数3以上）も同じ扱いで置かれている（`docs/decisions/09-stagnation.md` の末尾）。

### 判断3. 配布物にバージョンの刻印を入れるか → **単独の判断としては消える（導出。2026-09-01）**

工程4（追随）は、導入先が「自分がどの版の配布物を使っているか」を言えないと成立しない。本記録は当初、これを PO へ問うべき3件目として残していた。**しかし、その前提が変わった。**

**配布方式は既に決着している** —— `docs/decisions/24-distribution-plugin.md`（PO 判断 2026-08-28）が、配布方式を手動コピーから **Claude Code のプラグイン機構**（スキル・担当エージェントの定義・フックをまとめて配り、有効化するだけで入る仕組み）へ移すことを確定させた。**プラグインは版という概念を仕組みとして持つ**ため、刻印のために `org/VERSION` のような独自のファイルを別途作るのは、要件定義書 §18（独自の設定ファイルを作らない）に照らして筋が悪い。

**したがって、この論点は「刻印を入れるか否か」という独立した判断ではなくなり、プラグイン移行の調査項目のひとつになる。** 具体的には、24 の §D が挙げた調査（`docs/experiments/03-plugin-distribution.md` に記録票として残す予定のもの）へ、**「導入先が、自分がどの版の配布物を使っているかを言えるか」を確かめる項目として足す。**

**これは既存の確定事項からの導出であって、PO の回答を得たものではない**（本リポジトリの確定作業の規約に従い、導出であることを明記する）。**異論があれば差し戻せる。** 差し戻す場合の代替は、当初案どおり「移行を待たずに先に刻印だけ入れる」である。

**暫定期間中の扱い。** 24 の §F が定めるとおり、移行が済むまでは手動コピーで運用を続ける。**その間、追随は今までどおり人の記憶に頼る**——これは移行までの既知の弱点として残る。

---

## J. やらないこと

**過剰に作らない**（PO の作業スタイル）。この文書の範囲外とするものを明示する。

- **提案の自動適用。** 人間の承認でループを閉じる（§B-2）
- **試験用課題の集合（eval）の基盤。** 失敗事例が20〜50件溜まるまで作らない（§B-2）
- **定期実行の外部機構。** §C-5 の代替で足りる
- **改善エージェントに編集系ツールを与えること。** §H の1 で構造的に防ぐ
- **ロールをさらに増やすこと。** 6体目で止める。次に「足りない」と感じたら、まずスキルを疑う（要件定義書 §17.3）
- **改善エージェントが個々の担当エージェントを評定すること。** 対象は仕組みであって担当者ではない（§D-2）
- **層3（開発対象アプリの利用者向け文書）への関与。** ドキュメントエージェントの担当である

---

## K. 欠けていた4点を、この設計のどこで埋めるか

| 欠けていたもの（§A-2） | 埋める箇所 |
| --- | --- |
| 1. 2回目以降の点検が無い | §C-5 の起動条件3つ ＋ §H の7（検査の出力に「前回から N 件」を足す）／9（初回点検の手順を繰り返し可能に改訂） |
| 2. 層2 → 層1 の還流経路が無い | 工程4（§C-3）＋ 提案の `適用範囲` 欄（§F-2）＋ 重さ`重`の承認経路（§F-3）。**配布方式そのものは `docs/decisions/24-distribution-plugin.md` で決着済み**（プラグイン機構へ移す） |
| 3. 組織の仕組みへの異議を上げる先が無い | §E-2 の `## 組織への指摘` 節（エスカレーションとは別物として定義）＋ §H の4・5・7 |
| 4. 改善の判断の記録先が無い | §F-1 の改善提案の台帳（§I 判断1）。決定ログの索引（`docs/decisions/21-decision-log-index.md`）が実装されたら、`対象` 欄を「組織」として索引へ載せる |

---

## L. 要求（22）の6論点に、この設計がどう答えているか

`docs/decisions/22-org-self-improvement.md` の §D は、**設計を進めるために潰すべき論点を6つ挙げ、選択肢だけを並べて推奨を書かずに残していた。** 本記録がそれぞれにどう答えたかを対応させる。**「答えていない」ものも隠さずに書く。**

| 22 の論点 | 本記録の答え | どこに書いたか | 十分か |
| --- | --- | --- | --- |
| **1. 改善提案を、どこへ、どの形で出すか**（案a キューに種別を足す／案b 専用台帳／案c 既存記録の節） | **案b。専用台帳 `docs/org-improvements.md` を新設する**（開発対象リポジトリに置くファイルの7種類目）。案a を採らないのは、ゴール健全性評価が「タスクは消化されているのにゴールへ近づいていない」を検知する仕組みであり、そこへ組織改善のタスクが混ざると検知が効かなくなるため | §F-1、§I 判断1（**PO 承認 2026-09-01**） | **足りている** |
| **2. 誰が上げるか。どの粒度で上げるか**（担当エージェントが直接書く／司令塔がまとめる） | **両方を分業させる。** 担当エージェントは自分のタスクファイルへ1行書くだけ（作業を止めない・衝突しない）。**台帳へ起票するのはオーケストレーターだけ**。「複数の指摘を1課題にまとめる」のは判断であり機械にも担当エージェントにもできない | §E-2、§F-1-a（**PO 決定 2026-09-01**） | **足りている** |
| **3. 「課題である」と言うための基準**（既存の数値に紐づける／自由記述で PO が判断） | **既存の測れる信号に紐づける。** 観測する信号を9種に固定し、うち6種は既存のスクリプトが出す数字である。加えて**提案には「期待する効果」を必須欄にし、どの指標がどう動けば成功かを書けない提案は受け付けない**。優先度は改善エージェントがつけ、基準の1つは「同じ趣旨の指摘が3件以上の別タスクから出ているか」 | §E-1、§F-1-b、§F-2 | **足りている。** ただし 22 が試金石とした「『質問が逼迫している』を数字で言えるか」は、**PO確認待ちキューの件数と滞留日数で言える**——これは実測で確かめていない |
| **4. いつ出すか**（完了報告に混ぜる／セッション末／N件ごと／閾値超過で自動催促） | **3条件のいずれかで点検を起動する**（§C-5）。定期実行の外部機構は作らない——要件定義書 §3.3 が完全自動起動を MVP 対象外としているため、**セッション開始時に既に走っている検査へ「前回の点検から何件完了したか」を1行足す**だけで、呼び出し時期が目に入る形にする | §C-5、§H の7 | **足りている。** ただし件数の閾値（暫定5件）は PO の明示的な回答を得ていない（§I 判断2） |
| **5. プロジェクト固有の改善と、配布元へ戻す改善をどう分けるか** | **提案に `適用範囲` 欄を必須にし、工程4（追随）をループの一部として定義する。** 層1（配布物）を直さないと全導入先で再発するものは、重さ `重` として PO確認待ちキューへ上げる。**この工程はこの組織に固有であり、外の既成解には無い**——組織の定義が層1と層2に分かれている以上、片方だけ直しても他の導入先で再発するため | §C-3（工程4）、§F-2、§F-3 | **足りている。** ただし**還流の実行そのもの**（配布元をどう直し、導入先へどう届けるか）は配布方式に依存し、`docs/decisions/24-distribution-plugin.md` の移行が済むまで人手のままである |
| **6. マインドセットをどこに書くか**（層2の規約へ置く／点検の手順に入れる） | **書式と3種別を層2の規約へ置き（§H の5）、書く場所をタスク別ファイルの雛形へ実物として入れる**（§H の4。**文章で規定するだけで雛形を配らないのは、本リポジトリが既に踏んだ失敗である**） | §E-2、§H の4・5 | **足りていない可能性が高い。** 下記参照 |

### L-1. 論点6 は、まだ十分に答えていない → **2026-09-02 に §M で埋めた**

**以下の3点は、この文書を書いた時点（2026-09-01）の評価である。** その後の改訂（§M）で、**3点とも埋めた。** 何をどう埋めたかは §M-1 と §M-2 にある。**この節は、何が足りなかったかの記録として残す。**

**22 が求めているのは「すべての担当エージェントが課題意識を持つ」ことであり、「書く場所と書式がある」ことではない。** 本記録の答えは後者に寄っている。

具体的に、次の3点が埋まっていない。

1. **課題意識そのものを、5つの担当エージェントの定義へ書いていない。** 現状では、層2の規約に書式が載り、タスク別ファイルの雛形に空の節があるだけである。**規約は強制力を持たない**（Claude Code の制約。`CLAUDE.md` はコンテキストであって設定ではない）。**空欄が用意されているだけで書かれる保証は無い**
2. **「新しいロールが要る」という提案の扱いを、経路として書いていない。** 22 はこれを対象に含めると明記しており、PO が例として挙げたのもこの形（「質問が逼迫して司令塔が回らないので、質問をさばく役を置きたい」）である。本記録の3種別（`規約` / `道具` / `反復`）には**ロールの過不足を書く枠が無い**
3. **「プロジェクトごとに最適化された組織になっていく」という到達点を、指標として持っていない。** 効果の照合（§G）は個々の改善が効いたかを見るものであり、**組織がその導入先に馴染んできたかを見るものではない**

**この3点が、改善エージェントの作り込みで最初に扱うべき材料である。**

**→ 3点とも 2026-09-02 に埋めた。**

| 足りなかった点 | どう埋めたか |
| --- | --- |
| 1. 課題意識を、担当エージェントの定義へ書いていない | **3層で担保した**（§M-2）——規約ファイルの新設（`org/rules/org-improvement-feedback.md`）／層2の運営規約への入口3行／**雛形に空欄を実物として配る**。5つの定義へ複写はしない（確定事項に反するため）。加えて**改善エージェント側に「指摘が0件なのは経路が死んでいる可能性のほうが高い」という検知**を持たせた |
| 2. 「新しいロールが要る」を書く枠が無い | **指摘の4つ目の種別 `ロール` を新設**（§M-1 の3）。書く条件はチーム構成の設計論の6つの引き金。**結論ではなく観測した事実を書かせる。** 役を増やすのは7段のはしごの6段目であり、段1〜5 で解けないことを先に示させる（§M-1 の2） |
| 3. 到達点を指標として持っていない | **組織がその導入先に馴染んできたかを、4つの量で見る**（§M-1 の6）。手順スキルに段5.5 を新設し、改善エージェントの出力へ O-4 を追加 |

---

## M. 改訂（2026-09-02）— 2回目の外部調査を受けた作り込み

**きっかけ:** PO の指示——「世の中のループ開発における改善エージェントのベストプラクティスをリサーチし、それはソフトウェア開発向けに特化した改善スキームだと思うので、組織開発としてどう応用するかを考えること」。

**調査の記録は `docs/experiments/05-improvement-agent-practices.md`**（外部の出典5件、現物調査8件、突き合わせで一致8点・食い違い6点・欠落5点）。ここには採否だけを書く。

**4工程（観測 → 診断 → 反映 → 追随）も、改善エージェントを6体目とする構成も、承認の3段も変えていない。** 外部の出典と照らして、これらはむしろ裏付けが取れた（`05` §3-1 の一致8点）。**変えたのは、改善エージェントが何を見て、どう直し方を選ぶかである。**

### M-1. 採ったもの（6件）— いずれも導出。PO の回答を要していない

| # | 採ったもの | 出所 | どこへ入れたか |
| --- | --- | --- | --- |
| 1 | **報告だけで評価しない。** 担当エージェントが書いた報告と、実際に費やされた量を突き合わせる | 「生のログと元の出力から評価せよ。モデルが書いたそれらの報告からは決して評価するな」（`05` §2-2） | 信号を9種から**10種**へ。`org/skills/org-improve/SKILL.md` 段2 に「信号10 — 報告だけで判断しない」を新設 |
| 2 | **直す場所を7段のはしごとして明示し、最も低い段で直す** | 「再発する失敗は、その修正を表現できる最も低い段で直す」（同上） | 同スキルに **段3.5** を新設。指摘を書く側の規約（`org/rules/org-improvement-feedback.md`）にも同じはしごを載せた |
| 3 | **`ロール` を、指摘の4つ目の種別として足す。** 書く条件は6つの引き金 | Team Topologies 第8章の6つの引き金（`05` §2-4） | `org/templates/T-XXX.md` と新設の規約。**要求（22）が対象に含めていたのに、枠が無かった**（`05` 事実3） |
| 4 | **`撤回` を、台帳の9つ目の状態として足す** | ACE の「害になった回数で退役させる」（`05` §2-1） | `org/templates/org-improvements.md`。**`効果確認済み` が終端で、戻す経路が無かった**（`05` 事実2） |
| 5 | **改善を反映した箇所に、課題の番号を刻む**（`(OI-007)` の形） | ACE の「番地を付けて、あとから指せるようにする」「出自を辿れるようにする」（同上） | 同スキル 段5 と台帳の雛形。**辿れない規約は、効かないと分かっても戻せない** |
| 6 | **組織がその導入先に馴染んできたかを、4つの量で見る** | Toyota Kata の「目標状態を先に定める」（`05` §2-5） | 同スキルに **段5.5** を新設し、改善エージェントの出力へ **O-4** を追加。**要求（22）の到達点に、初めて測れる形を与えた** |

**この6件はいずれも、既存の確定事項と外部の出典から導ける。** §L-1 が「答えられていない」と認めた3点のうち、**2点目（ロールの提案の枠）と3点目（到達点の指標）は、これで埋まった。**

### M-2. 1点目（すべての担当エージェントが課題意識を持つ）をどう埋めたか

**要求（22）は「マインドセットとして定義に書く」と述べていた。しかし、5つの担当エージェントの定義へ同じ文章を複写することはしない。**

理由は、既に確定している制約に反するためである——**組織共通の規約は層2に置けば全担当エージェントが参照する。エージェント定義ごとに重複させない**（要件定義書 §22-12 のマッピング、および Claude Code の制約3）。複写すると、直すたびに5箇所を直すことになり、必ず一部が古くなる。

**代わりに、次の3層で担保する。**

| 層 | 何を置いたか | なぜそこか |
| --- | --- | --- |
| **規約**（`org/rules/org-improvement-feedback.md`。新設） | なぜ書くのか（**気づくのは必ず作業しているあなたである**）、どこに書くか、いつ書くか、4種別と書式、`ロール` の6つの引き金、書かなくてよいもの | 全担当エージェントに共通し、かつ常時読む必要は無い。**トピック別の規約の置き場である** |
| **層2の運営規約**（`org/CLAUDE.md`）への3行 | 「組織への気づきはエスカレーションではない。作業を止めずに1行書く。台帳へは自分で書きに行かない。詳細は規約ファイルへ」 | **常に知っていなければ、規約ファイルの存在に気づけない。** 内容は置かず、入口だけを置いた |
| **雛形**（`org/templates/T-XXX.md`） | `## 組織への指摘` 節を、4種別の説明と例つきの実物として配る。既定値は `無し` | **規約は強制力を持たない**（Claude Code の制約5）。**目の前のファイルに空欄があることが、文章の規約より強く効く** |

**「規約に書けば守られる」に頼っていない**——これは外部の出典が「プロンプトの文章に書いた制約は消える。実行基盤の側で強制せよ」と述べている点と同じ考え方である（`05` §2-2 の5）。

**加えて、改善エージェントの側に検知を持たせた。** 数タスク回っても指摘が1件も上がってこないとき、**それを「問題が無い」と読まず、「書く仕組みが働いていない可能性のほうが高い」として報告する**（スキル段2に新設、およびエスカレーション条件に追加）。**経路が死んでいることを、経路の外から見る。**

### M-3. 採らなかったもの（2件）

| 採らなかったもの | なぜ |
| --- | --- |
| **提案役が見ていない分を含む2分割の測定で、変更の受け入れ可否を判定する**（`05` §2-2） | **母数が無い。** トライアル先の完了タスクは数件規模である。これは §B-2 で試験用課題の集合を作らないと決めたのと同じ理由であり、判断を変えない |
| **強化学習で構成そのものを自動で組み替える**（`05` §2 の周辺研究） | 要件定義書 §3.3 が完全自動起動・24時間無人運転を MVP 対象外としており、**人の承認でループを閉じる**という §B-2 の確定事項に正面から反する |

### M-4. 新たに PO の判断が要る点（2件）

**推測で埋めない。** `05` の突き合わせで出た食い違いのうち、**導出では決められないものが2件残る。**

#### 判断4. 層2の運営規約が、すでに行数の上限を超えている

**事実。** 配布物の層2の運営規約（`org/CLAUDE.md`。開発対象リポジトリのルート `CLAUDE.md` へ**追記**される内容）は **224 行**ある。**毎セッション全文が読み込まれ、200 行を超えると遵守率が落ちる**というのが、調査で確認済みの Claude Code の制約である（`docs/decisions/00-research-notes.md`）。しかも実際に読み込まれるのは、導入先の既存の内容を足した全文であり、これより長い。

**これは今回の改訂で生じたものではない。** 改訂前が 222 行で、今回の追記は3行である。**既に超えていたところへ、さらに足した。**

**そして、この問題は改善が積み上がるほど悪化する。** 改善提案が層2の規約へ反映されるたびに規約は伸び、伸びるほど守られなくなる。**つまり、改善のループそのものが、自分の効き目を削る構造を持っている。**

段3.5（7段のはしご）は、**手順や判断基準はスキル側（呼ばれたときだけ読まれる）へ寄せ、運営規約へ置くのは全ロールが常に知っていなければならない事実だけにする**という方針を入れた。**しかしこれは、これから足すものに効く規則であって、既にある 224 行を減らすものではない。**

| 案 | 内容 | 利害 |
| --- | --- | --- |
| **A（推奨）** | **層2の運営規約の棚卸しを、独立した課題として起票する。** いま入っている内容を「常に必要な事実」と「呼ばれたときでよい手順」に仕分け、後者をスキルまたはトピック別の規約へ移す | **移すのであって、縮めない。** 書き直して短くすると具体が落ちて一般論へ潰れる（`05` §2-1）。棚卸しには手間がかかるが、これは組織の全タスクに効く土台である |
| B | いまは何もしない。改善が積み上がって実害が出てから対処する | 安いが、**実害は「規約が守られない」という最も切り分けにくい形で出る。** 原因の特定に時間がかかる |
| C | 200 行の制約を、この組織では受け入れないと決める | **制約は好みではなく確認済みの事実である。** 受け入れないという選択は取れない |

**A を推す。** ただし棚卸しは相応の作業量になるため、**いつやるかは PO の判断である。**

#### 判断5. 会話の記録そのものを、改善エージェントに読ませるか

**信号10（報告と実際の量の食い違い）は、いまの実装ではトークン集計の出力、つまり集計後の数字だけを見る。** 実際のやりとりの中身までは読まない。

**外部の出典は、生の実行記録に全面的にアクセスできる提案役が、数字だけを渡された提案役より明確に優ったという証拠を挙げている**（`05` §2-2）。しかし、そこまで踏み込むには判断が要る。

- **会話の記録は、開発対象リポジトリの外にある**（この実行環境の `~/.claude/projects/` の下）。担当エージェントがリポジトリの外を読むことになる
- **中身には、PO とのやりとりがそのまま含まれる**
- **読む量が大きく、コンテキストを圧迫する**

| 案 | 内容 |
| --- | --- |
| **A（推奨）** | **いまの形（集計後の数字で食い違いを見る）で始め、実測してから判断する。** 食い違いの検出が実際に役に立ったかを、数回の点検の後に見る |
| B | 生の記録を読む経路を今すぐ足す。範囲を「直近 N 件の完了タスクに対応する部分だけ」に限る |
| C | 読ませない。集計後の数字だけで運用すると決める |

**A を推す。** 判断2（点検の起動間隔）と同じ扱いで、**暫定の形を置いて実測で見直す。**

### M-5. この改訂で触ったファイル

| ファイル | 何を変えたか | 新規 |
| --- | --- | --- |
| `org/rules/org-improvement-feedback.md` | 組織への指摘の書き方（全担当エージェント向けの規約） | **新規** |
| `org/agents/org-improvement.md` | 課題意識は全員のもの／第三者として実行記録を読む／報告だけで判断しない／役の分け方の点検（責務6）／入力 I-3／出力 O-4／エスカレーション条件2件 | — |
| `org/skills/org-improve/SKILL.md` | 信号10種へ／指摘0件の読み方／信号10の節／症状表に1行／**段3.5（7段のはしご）**／段5の欄を8つへ＋出自の刻印／**段5.5（組織の目標状態）**／やってはいけないこと5件追加／考え方4件追加 | — |
| `org/templates/T-XXX.md` | `## 組織への指摘` に `ロール` 種別と、結論を書かない旨 | — |
| `org/templates/org-improvements.md` | 状態 `撤回` を追加／`効果確認済み` は終端ではない／番号を刻む／**見本の1行が確定済みの決定と食い違っていたのを修正**（`05` 事実7） | — |
| `org/CLAUDE.md` | 組織への指摘の入口3行 | — |
| `org/README.md` | 配布物の一覧へ4件を追加（**改善エージェント・手順スキル・台帳の雛形が、そもそも一覧に載っていなかった**） | — |
