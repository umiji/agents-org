# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## このリポジトリは何か

`agents-org` は、**Claude Code 上で自律的にアプリケーション開発を進める「AI開発組織」そのものを定義するリポジトリ**である。

重要な区別:

- ここで作るのは **組織の定義**（エージェント、スキル、タスク管理規約、エスカレーション経路）であって、組織が作る**成果物としてのアプリケーション**ではない。
- 「Webアプリを作れ」という指示が来た場合、それは通常このリポジトリへの実装依頼ではなく、**この組織を使って別のプロジェクトを動かす**という意味である可能性が高い。判断がつかない場合は確認すること。

## 現在の状態（2026-08-25 時点）

**要件定義書 v0.2 まで完了。配布物も揃い、トライアル運用が始まっている。**

- 要件定義書 v0.1 §23 が定めた12項目の確定作業は完了し、**v0.2 の本文へ取り込んだ**（後述）。
- **`org/` に組織の実行時ファイルが入っている** — 層2の規約、用語集、5ロールのエージェント定義、オーケストレーターとセッション再開のスキル、台帳の雛形、停滞検知スクリプト。開発対象リポジトリへコピーして使う（`org/README.md`）。
- **PO が別の開発対象リポジトリ（SNS Agent）でトライアル運用中。** そこのオーケストレーターからの差し戻しを受けて、配布物へ修正を入れている（2026-08-25 に6件）。**組織が実際に動いて初めて分かることが、ここから入ってくる。**
- **本リポジトリ自体にはビルド対象が無い。** 唯一の実行可能物は配布用のスクリプト1本である。

## よく使うコマンド

配布物のスクリプト `org/scripts/org-check.py` は、開発対象リポジトリのタスク台帳を検査するものであり、本リポジトリには検査対象が無い。動作を確かめるときは、台帳を持つディレクトリを `--root` で指定する。

```sh
python3 org/scripts/org-check.py --root <台帳のあるリポジトリ>              # 停滞検知と整合性検査
python3 org/scripts/org-check.py --root <...> --summary                    # ゴール健全性の8指標
python3 org/scripts/org-check.py --root <...> --today 2026-08-20 --days 3  # 基準日と日数を変える
python3 -c "import ast;ast.parse(open('org/scripts/org-check.py',encoding='utf-8').read())"  # 構文確認
```

`python3` というコマンド名が無い環境（Windows では `python` だけのことが多い）では読み替える。セッション開始時に検査を走らせる設定（`org/settings.snippet.json`）は、`python3` を試して失敗したら `python` を試す形にしてある。

**Python 3.8 以降、標準ライブラリのみ。** 任意の開発対象リポジトリへ配るため、追加インストールを要求しない。

## 正典ドキュメント

- `docs/requirements/ai-org-mvp-v0.2.md` — **AI開発組織 MVP 詳細要件定義書 v0.2。これが現行版**
- `docs/requirements/ai-org-mvp-v0.1.md` — 前版（Draft）。**履歴として残してあるだけ。参照しない**
- `docs/decisions/00-research-notes.md` — ベストプラクティス調査メモ（決定の根拠。Claude Code の機能制約を含む）
- `docs/requirements/v0.1-changes.md` — v0.1 から v0.2 へ何がどう変わったかの記録（34項目。**反映済み**）
- `docs/glossary.md` — **用語集。** 組織上の役割を指す語と、Claude Code という道具の機構を指す語を区別する。**新しい語を使う前にここを見る**

このリポジトリでの作業を始める前に、必ず全文を読むこと。以下はその要約ではなく、**作業中の判断を拘束する制約**の抜き出しである。判断に迷ったら要件定義書の本文が優先する。

## 拘束力のある設計制約

要件定義書 §4 / §18 / §19 / §24 より。これらに反する提案は、たとえ技術的に優れていても採らない。

1. **Claude Code 標準機能を最大限使う。** CLAUDE.md と Skills で足りることに独自の仕組みを作らない。
2. **独自のJSON/YAML設定ファイルを作らない。** 作ってよいのは §18 が挙げる条件（Claude Code 以外の実行基盤への対応、複数実行環境の抽象化、機械的な読み書きが必要な構造化設定、自然言語では管理しきれないワークフロー）に実際に到達したときだけ。「将来必要になりそうだから」は理由にならない。
3. **既存拡張を壊さない。** Everything Claude Code / Superpowers 等が導入済みの環境で共存する前提。既存の commands / skills / hooks を上書き・削除しない。組織独自の定義は独立した領域に置く。
4. **オーケストレーターは実装しない。** 司令塔に徹する。コーディング・テスト実装・通常のドキュメント作成・通常のレビューは担当外（§6.3）。
5. **サブエージェント間の自由な通信はしない。** オーケストレーターをハブとする。例外は承認済みの定型ワークフロー（実装→レビュー→実装のループ等）のみ（§14）。
6. **エージェントは分からないことを無理に決めない。** 自分の担当範囲 → 許可された他エージェントへ相談 → オーケストレーター → PO確認待ちキュー、の順にエスカレーションする（§12）。
7. **PO確認待ちの間も、依存関係のないタスクは止めない。**（§13）
8. **単純な繰り返し処理はスクリプトへ切り出す。** LLMのトークンは判断・設計・問題解決に使う（§4.5）。
9. **拡張はファイル追加で済むように。** 新しい専門エージェントは、理想的には定義ファイルを1つ足すだけで組織に加われる構造を目指す（§4.4 / §17.1）。そのために**組織側でエージェント一覧を持たない**（`docs/decisions/12-agent-addition.md`）。
10. **ロールは責務の単位であって、知識の単位ではない。** 専門知識が要るだけならスキルを足す。ロールを増やさない（§17.3 / `12`）。

## Claude Code の制約（設計を規定する事実）

調査で確認した事実。根拠と詳細は `docs/decisions/00-research-notes.md`。**これらは好みではなく制約であり、設計はこれに従う。**

1. **サブエージェントは `AskUserQuestion` を使えない。** PO への問い合わせはメインセッションからしか行えない。PO確認待ちキューをオーケストレーターが一元管理するのは技術的必然。
2. **サブエージェントは会話履歴・auto memory を継承しない。** 作業指示は自己完結でなければならない。「さきほどの方針で」は通じない。
3. **CLAUDE.md 階層はサブエージェントへ継承される。** 組織共通の規約はここに置けば全エージェントが参照する。エージェント定義ごとに重複させない。
4. **CLAUDE.md は毎セッション全文ロードされ、200行を超えると遵守率が落ちる。** 長い手順は Skills（呼ばれた時のみロード）へ、トピック別規約は `.claude/rules/` へ出す。
5. **CLAUDE.md は強制力を持たない**（コンテキストであって設定ではない）。必ず実行させたい処理は hook にする。
6. **オーケストレーターのコンテキストは報告のたびに膨張する。** 報告は要約とし、成果物は参照（パス・コミット）で渡す。

### §22-12「Claude Code 標準機能へのマッピング」の暫定解

| 置くもの | 置き場所 |
| --- | --- |
| 組織共通の規約（常に必要な事実） | `CLAUDE.md` |
| トピック別・パス限定の規約 | `.claude/rules/*.md` |
| 各ロールの定義 | `.claude/agents/*.md` |
| 多段階の手順・チェックリスト | `.claude/skills/*/SKILL.md` |
| 必ず実行させる処理（停滞検知・整合性検査） | hooks + スクリプト（§4.5） |
| 組織の状態（タスク・PO確認待ち・引き継ぎ） | **開発対象リポジトリ**の `docs/` 配下（07 / 08 / 11） |

## 文書の3層構造

「ドキュメント」が指すものは層で異なる。混同しないこと（`docs/decisions/06-documentation-agent.md`）。

| 層 | 内容 | 読み手 | 保守 |
| --- | --- | --- | --- |
| 層1 | **本リポジトリ**の要件定義・決定記録 | PO | PO と支援セッション |
| 層2 | 開発対象リポジトリの `CLAUDE.md` / `.claude/rules/` / エージェント定義 | AI組織自身 | オーケストレーター |
| 層3 | 開発対象アプリの成果物ドキュメント | AI組織 と 人間 | ドキュメントエージェント |

層3 はさらに2階建て。**エージェント用マスタ**（機能・目的・シーン単位に細かく分割。正典）と、そこから**スクリプトで生成する人間用ビュー**（結合された通し読み用）。マスタだけが真実であり、人間用ビューは手で書かない。

## MVPスコープ外（§3.3）

拡張性は考慮するが、**MVPでは作らない**: 非開発業務（SNS投稿等）、Claude Code 新規セッションの完全自動起動、24時間無人運転、Webダッシュボード、高度なKPI分析基盤、エージェント間の自由なネットワーク型通信、複数AIプロバイダー横断の実行基盤。

セッション引き継ぎは **MVPでは手動**（§15）。自動セッション監視・自動引き継ぎを実装しない。

## ディレクトリ規約

**本リポジトリ**（層1）:

```
docs/handover.md     このリポジトリでの作業の引き継ぎ記録。新しいセッションが最初に読む
docs/glossary.md     用語集（正典）。語の定義を変えたら、その語を使う決定記録も直す
docs/requirements/   要件定義書と、その差分一覧（正典。版を切って残す）
docs/decisions/      確定した仕様・設計判断の記録
org/                 組織の実行時ファイル。開発対象リポジトリへ配置する配布物（org/README.md）
```

`docs/handover.md` は**このリポジトリ（層1）の作業**の引き継ぎであり、AI組織が開発対象リポジトリに書く引き継ぎ記録とは別物である。同じファイル名だが、置かれるリポジトリが違う。

**`org/` を本リポジトリのルート `.claude/` へ置かない。** 層2の `CLAUDE.md` は開発対象リポジトリのルートに置かれるものであり、層1の `CLAUDE.md`（このファイル）と同じ場所には置けない。理由は `13` の B。

`docs/decisions/` には、要件定義書 §22「今後決定する事項」および §23 の手順に従って確定した内容を書く。**未確定の事項を推測で埋めない。** 確定していないものは確定していない状態のまま残すのが正しい。

**開発対象リポジトリ**（層2・層3。ここに作るものではない。組織が動く先に作られる）:

```
docs/task-list-{project-name}.csv   タスク索引（07）
docs/tasks/T-XXX.md                 タスク別の詳細記録。正典（07）
docs/po-queue.md                    PO確認待ちキュー（08）
docs/handover.md                    セッション引き継ぎ記録（11）
```

**この2つを混同しない。** 本リポジトリにタスク台帳を作るのは、この組織を使って本リポジトリ自身を開発対象にすると PO が決めたときだけである。

## §23 の仕様確定 — 完了

要件定義書 §23 が定めた12項目の確定作業は **すべて完了した**（2026-08-20）。

| # | 対象 | 記録 |
| --- | --- | --- |
| 1〜6 | 各エージェントの ミッション → 責務 → 入力 → 出力 → エスカレーション条件 | `docs/decisions/01`〜`06` |
| 7 | タスク管理仕様 | `docs/decisions/07-task-management.md` |
| 8 | エスカレーション・クライテリア | `docs/decisions/08-escalation-criteria.md` |
| 9 | 停滞管理 | `docs/decisions/09-stagnation.md` |
| 10 | ゴール健全性評価 | `docs/decisions/10-goal-health.md` |
| 11 | セッション引き継ぎ | `docs/decisions/11-session-handoff.md` |
| 12 | エージェント追加方式 | `docs/decisions/12-agent-addition.md` |
| （追加） | 実行形態と配置 | `docs/decisions/13-execution-form.md` |
| （追加） | 未決の論点（選択肢と推奨案つき。**確定事項ではない**） | `docs/decisions/14-open-questions.md` |
| （追加） | ブランチ運用と並列実行 | `docs/decisions/15-branch-and-parallel.md` |

`13` は §23 の12項目には無いが、エージェント定義ファイルを書くために必要だったため確定させた。**オーケストレーターはメインセッションであり、サブエージェントではない**（`00` §1.1 と `08` からの帰結）。

要件定義書 v0.1 から変更・確定した事項は `docs/requirements/v0.1-changes.md` に一覧してある。**次版を切るときはそこから取り込む。**

### 次にやること

**引き継ぎの詳細は `docs/handover.md` にある。新しいセッションはそこを先に読むこと。**

1. **トライアル先へ、修正した配布物を反映する** — 2026-08-25 の修正6件が届いていない。手でコピーし直す。**停滞検知スクリプトは、トライアル先が同じ不具合を独自に直しているため突き合わせが要る**
2. **残っている不足** — ツールの実行許可の推奨値が配布物に無い／専門知識のスキルが0本／停滞検知スクリプトの回帰テストが無い／人間用ビューの生成スクリプトが無い／**リリース・デプロイの工程が組織に存在しない**（PO の判断が要る）
3. **配布方式の判断**（PO）— トライアルが2つ目の導入先になり、**更新の追随ができない問題が実際に起きている**（`docs/decisions/14-open-questions.md` の論点1）

**済んだもの**: 要件定義書 v0.2、エージェント定義（`org/agents/`）、スキル3本（`org/skills/`）、層2の規約と用語集（`org/CLAUDE.md`・`org/rules/`・`org/glossary.md`）、台帳の雛形（`org/templates/`）、停滞検知・整合性検査スクリプト（`org/scripts/org-check.py`）。

### 確定作業の進め方（新しい論点が出たときも同じ）

- 入力・出力は**情報項目のレベル**で定義する。実行形態（サブエージェント構成）には踏み込まない。
  ただし**保存形式は 07 で確定した**（CSV索引 + タスク別Markdown）。09・10 の集計をスクリプト化するために必要だったため、01 が保留していた前提をここで解いている。
- 選択肢が複数ある論点は、**選択肢・各案の利害・推奨案**を添えて PO に問い、回答を決定記録へ残す。推測で埋めない。
- 既存の確定事項から一意に導ける論点は、PO へ問わず確定させてよい（`05` / `08` / `12` がこの方式）。その場合は**導出であることを明記し、異論があれば差し戻せる形にする**。

## PO（プロジェクトオーナー）との関係

このリポジトリの人間の所有者は PO である。PO はゴールと判断を提供し、細かな作業指示は出さない前提で設計されている（§1）。仕様確定の場面で選択肢が複数ある場合は、**選択肢・各案の利害・推奨案**を添えて問う（§13 の確認待ち項目の形式に準じる）。

## 言語と書き方

要件定義書・設計ドキュメント・PO とのやりとりは**日本語**。

**ハイコンテキストに書かない。** コマンド名・ツール名・設定項目名・ドキュメントの章番号・略語を「名前だけ」で置かず、それが何であり何をするのかを必ずその場で説明する。読み手が文を理解するために別の資料を開かなければならないなら、その文は書けていない。

適用先はチャットの返答だけでなく、コミットメッセージ・決定記録・プルリクエストの本文を含む。詳細と書き換えの例は `.claude/skills/low-context-communication/SKILL.md`。

**語は `docs/glossary.md` に合わせる。** 載せる基準は「その語を一般的な意味のまま使ったら、この組織では間違った行動になるか」。一般的な技術用語は載せない。

書いてはいけない語と、代わりに書く語:

| 書かない | 代わりに |
| --- | --- |
| サブエージェント（役割の意味で） | **担当エージェント**（サブエージェントは Claude Code の実行機構を指す語） |
| エージェント（5つだけを指すとき） | **担当エージェント** |
| ドキュメント（単独で） | 層1 / 層2 / 層3 のどれかを明示するか、「設計成果物」「利用者向け文書」 |
| ユーザー（POを指すとき） | **PO** |
| リポジトリ（単独で） | **本リポジトリ** / **開発対象リポジトリ** |
| タスクリスト | **タスク台帳** |
| 停滞（単独で） | 実時間基準の停滞 / サイクル基準の停滞 |

**「テスト」「レビュー」「完了」「push」など、一般語のまま使うと規約違反になる語がある。** 一覧と定義は用語集の分類1。
