# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## このリポジトリは何か

`agents-org` は、**Claude Code 上で自律的にアプリケーション開発を進める「AI開発組織」そのものを定義するリポジトリ**である。

重要な区別:

- ここで作るのは **組織の定義**（エージェント、スキル、タスク管理規約、エスカレーション経路）であって、組織が作る**成果物としてのアプリケーション**ではない。
- 「Webアプリを作れ」という指示が来た場合、それは通常このリポジトリへの実装依頼ではなく、**この組織を使って別のプロジェクトを動かす**という意味である可能性が高い。判断がつかない場合は確認すること。

## 現在の状態（2026-08 時点）

**要件定義フェーズ。実行可能なコードは存在しない。**

- ビルド／テスト／リントのコマンドは、まだ定義できる対象が無いため存在しない。
- エージェント定義ファイル（`.claude/agents/` 等）も**まだ存在しない**。これは未着手なのではなく、仕様が未確定だからである（後述）。

したがって、このファイルに「よく使うコマンド」の節はまだ無い。最初の実行可能物が入った時点で追記すること。

## 正典ドキュメント

- `docs/requirements/ai-org-mvp-v0.1.md` — **AI開発組織 MVP 詳細要件定義書 v0.1（Draft）**

このリポジトリでの作業を始める前に、必ず全文を読むこと。以下はその要約ではなく、**作業中の判断を拘束する制約**の抜き出しである。判断に迷ったら要件定義書の本文が優先する。

## 拘束力のある設計制約

要件定義書 §4 / §18 / §19 / §24 より。これらに反する提案は、たとえ技術的に優れていても採らない。

1. **Claude Code 標準機能を最大限使う。** CLAUDE.md と Skills で足りることに独自の仕組みを作らない。
2. **独自のJSON/YAML設定ファイルを作らない。** 作ってよいのは §18 が挙げる条件（Claude Code 以外の実行基盤への対応、複数実行環境の抽象化、機械的な読み書きが必要な構造化設定、自然言語では管理しきれないワークフロー）に実際に到達したときだけ。「将来必要になりそうだから」は理由にならない。
3. **既存拡張を壊さない。** Everything Claude Code / Superpowers 等が導入済みの環境で共存する前提。既存の commands / skills / hooks を上書き・削除しない。組織独自の定義は独立した領域に置く。
4. **オーケストレーターは実装しない。** 司令塔に徹する。コーディング・テスト実装・通常のドキュメント作成・通常のレビューは担当外（§6.3）。
5. **サブエージェント間の自由な通信はしない。** オーケストレーターをハブとする。例外は承認済みの定型ワークフロー（実装→レビュー→実装のループ等）のみ（§14）。
6. **エージェントは分からないことを無理に決めない。** 自分の担当範囲 → 許可された他エージェントへ相談 → オーケストレーター → PO確認待ちキュー、の順にエスカレーションする（§12）。
7. **PO確認待ちの間も、依存関係のないタスクは止めない。**（§13）
8. **単純な繰り返し処理はスクリプトへ切り出す。** LLMのトークンは判断・設計・問題解決に使う（§4.5）。
9. **拡張はファイル追加で済むように。** 新しい専門エージェントは、理想的には定義ファイルを1つ足すだけで組織に加われる構造を目指す（§4.4 / §17.1）。

## MVPスコープ外（§3.3）

拡張性は考慮するが、**MVPでは作らない**: 非開発業務（SNS投稿等）、Claude Code 新規セッションの完全自動起動、24時間無人運転、Webダッシュボード、高度なKPI分析基盤、エージェント間の自由なネットワーク型通信、複数AIプロバイダー横断の実行基盤。

セッション引き継ぎは **MVPでは手動**（§15）。自動セッション監視・自動引き継ぎを実装しない。

## ディレクトリ規約

```
docs/requirements/   要件定義書（正典。版を切って残す）
docs/decisions/      確定した仕様・設計判断の記録
```

`docs/decisions/` には、要件定義書 §22「今後決定する事項」および §23 の手順に従って確定した内容を書く。**未確定の事項を推測で埋めない。** 確定していないものは確定していない状態のまま残すのが正しい。

## 進行中の作業 — §23 の仕様確定

要件定義書 §23 が次に何をやるかを定めている。順序:

1. 各エージェントの「ミッション → 責務 → 入力 → 出力 → エスカレーション条件」を、この順に確定する
   1. オーケストレーター
   2. 設計エージェント
   3. 実装エージェント
   4. レビューエージェント
   5. テストエージェント
   6. ドキュメントエージェント
2. その後: タスク管理仕様 → エスカレーション・クライテリア → 停滞管理 → ゴール健全性評価 → セッション引き継ぎ → エージェント追加方式

要件定義書に既にあるのは **ミッションと責務まで**。**入力・出力・エスカレーション条件が埋めるべき空白**である。エージェント定義ファイルを書くのは、この確定作業が済んでからでよい。

### 確定状況

| # | 対象 | 状態 | 記録 |
| --- | --- | --- | --- |
| 1 | オーケストレーター | **確定** | `docs/decisions/01-orchestrator.md` |
| 2 | 設計エージェント | **確定** | `docs/decisions/02-design-agent.md` |
| 3 | 実装エージェント | **確定** | `docs/decisions/03-implementation-agent.md` |
| 4 | レビューエージェント | **確定** | `docs/decisions/04-review-agent.md` |
| 5 | テストエージェント | 未着手 | |
| 6 | ドキュメントエージェント | 未着手 | |
| 7〜12 | タスク管理仕様以降 | 未着手 | |

### 確定作業の進め方

- 入力・出力は**情報項目のレベル**で定義する。保存形式（ファイル形式）や実行形態（サブエージェント構成）には踏み込まない。PO の判断により、これらは意図的に未決のまま残している。要件定義書末尾「実装方式そのものを固定するものではない」に従う。
- 選択肢が複数ある論点は、**選択肢・各案の利害・推奨案**を添えて PO に問い、回答を決定記録へ残す。推測で埋めない。

### 要件定義書 v0.1 からの変更（次版へ反映すること）

- **§6.2 タスク項目に `完了条件` を追加**（2026-08-19 PO確定）。レビュー／テストエージェントが充足を判定する基準として必要。詳細は `docs/decisions/01-orchestrator.md` の確定事項 A。
- **§14 の直接通信の例外に「実装 → 設計 → 実装」を追加**（2026-08-19 PO確定）。ただし**軽微な問合せに限る**。設計変更を伴うものはオーケストレーター経由。判定基準は `docs/decisions/02-design-agent.md` の確定事項 C。
- **§8 / §10 のテスト責務の重複を解消**（2026-08-19 PO確定）。§8「必要なテスト実装」＝**単体テストに限定**、§10「テストコード作成」から単体テストを除く。詳細は `docs/decisions/03-implementation-agent.md` の確定事項 A。
- **§8「必要に応じた Git push」を具体化**（2026-08-19 PO確定）。commit は論理単位、push は**タスク完了時**（レビュー通過を待たない）。§15 のセッション断前提により、ローカルのみの状態を長く保たない。詳細は同ファイル確定事項 B。
- **§22-6「レビュー→実装の反復回数・終了条件」を確定**（2026-08-19 PO確定）。指摘は Blocker / Major / Minor の3段階。Blocker と Major が0件で通過、**2往復**で解決しなければオーケストレーターへエスカレーション。実装側は理由と根拠を付して反論でき、決着しなければオーケストレーターが裁定する。詳細は `docs/decisions/04-review-agent.md`。

## PO（プロジェクトオーナー）との関係

このリポジトリの人間の所有者は PO である。PO はゴールと判断を提供し、細かな作業指示は出さない前提で設計されている（§1）。仕様確定の場面で選択肢が複数ある場合は、**選択肢・各案の利害・推奨案**を添えて問う（§13 の確認待ち項目の形式に準じる）。

## 言語

要件定義書・設計ドキュメント・PO とのやりとりは**日本語**。
